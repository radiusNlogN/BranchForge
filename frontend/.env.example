# Copy to frontend/.env.local and adjust as needed. Contains no secrets.
#
# Base URL of the BranchForge backend. Must be one of the origins listed in the
# backend's CORS_ORIGINS, viewed from the browser's perspective.
VITE_API_BASE_URL=http://localhost:8000
